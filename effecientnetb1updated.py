# -*- coding: utf-8 -*-
"""effecientnetB1updated.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/12mGU0XYSIyWpLV_09RV1F4Gpb5kPiA5D
"""

# Commented out IPython magic to ensure Python compatibility.
import numpy as np
import tensorflow as tf 
from tensorflow import keras 
from tensorflow.keras.models import Sequential 
from tensorflow.keras.layers import Activation,Dense,Flatten,BatchNormalization,Conv2D
from tensorflow.keras.optimizers import Adam
from tensorflow.keras.metrics import categorical_crossentropy
from tensorflow.keras.preprocessing.image import ImageDataGenerator
from sklearn.metrics import confusion_matrix
import itertools
import os
import shutil
import random
import glob
import matplotlib.pyplot as plt 
import warnings
warnings.simplefilter(action='ignore',category=FutureWarning)
# %matplotlib inline

from tensorflow.keras.layers import MaxPool2D

train_path='/content/drive/MyDrive/trainfinal'
test_path ='/content/drive/MyDrive/test' 
val_path = '/content/drive/MyDrive/validation'

train_path='/content/drive/MyDrive/trainnew'
test_path ='/content/drive/MyDrive/testnew' 
val_path = '//content/drive/MyDrive/validationnew'

train_batches = ImageDataGenerator(preprocessing_function=tf.keras.applications.efficientnet.preprocess_input).flow_from_directory(directory=train_path,target_size=(224,224),classes=['below18','18to45','above45'],batch_size=64)
test_batches = ImageDataGenerator(preprocessing_function=tf.keras.applications.efficientnet.preprocess_input).flow_from_directory(directory=test_path,target_size=(224,224),classes=['below18','18to45','above45'],batch_size=64,shuffle=False)
val_batches = ImageDataGenerator(preprocessing_function=tf.keras.applications.efficientnet.preprocess_input).flow_from_directory(directory=val_path,target_size=(224,224),classes=['below18','18to45','above45'],batch_size=64,shuffle=False)

base_model = tf.keras.applications.EfficientNetB1(input_shape = (224, 224, 3),pooling=None, include_top = False, weights = 'imagenet')



last_few_layers = 10

for layer in base_model.layers[:329]:
    layer.trainable = False

sum=0

for l in base_model.layers:
    print(l.name, l.trainable)
    sum=sum+1
print(sum)

sum=0

for l in base_model.layers:
    sum=sum+1 
print(sum)

for layer in base_model.layers:
    layer.trainable = True

from tensorflow.keras.optimizers import Adam
from tensorflow.keras import layers
x = layers.Flatten()(base_model.output)
x = layers.Dense(1024, activation='relu')(x)
x = layers.Dropout(0.2)(x)

# Add a final softmax layer with 4 node for classification output
x = layers.Dense(3, activation='softmax')(x)

model = tf.keras.models.Model(base_model.input, x)

model.compile(optimizer = Adam(lr=0.0001), loss = 'categorical_crossentropy', metrics = ['acc'])

my_callbacks = [
    tf.keras.callbacks.ModelCheckpoint(filepath='/content/drive/MyDrive/modeleffnetb1updated.{epoch:02d}.h5'),
    tf.keras.callbacks.TensorBoard(log_dir='./drive/MyDrive/modeleffnetb1updated/logs'),
]

model.fit_generator(train_batches,validation_data=val_batches,steps_per_epoch = 198, epochs =3,callbacks=my_callbacks)

#model3=tf.keras.models.load_model('/content/drive/MyDrive/modeleffnetb1updated.03.h5')  80.7 test 78 val

import torch

path = '/content/drive/My Drive/model3' 
torch.save(model3, path)

path = '/content/drive/My Drive/model3'
model20=torch.load(path)

del model9

#model2=tf.keras.models.load_model('/content/drive/MyDrive/modeleffnetb1updated.03.h5') 80 test 80 val

predictions = model20.predict(x=test_batches,verbose=0)

cm = confusion_matrix(y_true=test_batches.classes,y_pred=np.argmax(predictions,axis=-1))

test_batches.class_indices

from sklearn.datasets import make_classification
from sklearn.metrics import plot_confusion_matrix

def plot_confusion_matrix(cm, classes,
                          normalize=False,
                          title='Confusion matrix',
                          cmap=plt.cm.Blues):
    """
    This function prints and plots the confusion matrix.
    Normalization can be applied by setting `normalize=True`.
    """
    plt.imshow(cm, interpolation='nearest', cmap=cmap)
    plt.title(title)
    plt.colorbar()
    tick_marks = np.arange(len(classes))
    plt.xticks(tick_marks, classes, rotation=45)
    plt.yticks(tick_marks, classes)

    if normalize:
        cm = cm.astype('float') / cm.sum(axis=1)[:, np.newaxis]
        print("Normalized confusion matrix")
    else:
        print('Confusion matrix, without normalization')

    print(cm)

    thresh = cm.max() / 2.
    for i, j in itertools.product(range(cm.shape[0]), range(cm.shape[1])):
        plt.text(j, i, cm[i, j],
                 horizontalalignment="center",
                 color="white" if cm[i, j] > thresh else "black")

    plt.tight_layout()
    plt.ylabel('True label')
    plt.xlabel('Predicted label')

cm_plot_labels = ['below 18','18to45','above 45']
plot_confusion_matrix(cm=cm,classes=cm_plot_labels,title='Confusion matrix')

i=0
j=0
sum=0

p=cm[0][0]+cm[1][1]+cm[2][2]+cm[3][3]
for i in range(0,4):
  for j in range(0,4):
    sum=sum+cm[i][j]
accuracy=(p/sum)
print(sum)
print(accuracy)

train_path2='/content/drive/MyDrive/train_tourist'
test_path2 ='/content/drive/MyDrive/test_tourist'

train_batches2 = ImageDataGenerator(preprocessing_function=tf.keras.applications.efficientnet.preprocess_input ).flow_from_directory(directory=train_path2,target_size=(224,224),classes=['ain dxb','atlantis','burj al arab','burj_khalifa'],batch_size=16)
test_batches2 = ImageDataGenerator(preprocessing_function=tf.keras.applications.efficientnet.preprocess_input).flow_from_directory(directory=test_path2,target_size=(224,224),classes=['ain dxb','atlantis','burj al arab','burj_khalifa'],batch_size=16,shuffle=False)

tourist_model = tf.keras.applications.EfficientNetB1(input_shape = (224, 224, 3),pooling=None, include_top = False, weights = 'imagenet')

for layer in tourist_model.layers:
    layer.trainable = True

from tensorflow.keras.optimizers import Adam
from tensorflow.keras import layers
y = layers.Flatten()(tourist_model.output)
y = layers.Dense(1024, activation='relu')(y)
y = layers.Dropout(0.2)(y)

# Add a final softmax layer with 4 node for classification output
y= layers.Dense(4, activation='softmax')(y)

model2 = tf.keras.models.Model(tourist_model.input, y)

model2.compile(optimizer = Adam(lr=0.0001), loss = 'categorical_crossentropy', metrics = ['acc'])

my_callbacks = [
    tf.keras.callbacks.ModelCheckpoint(filepath='/content/drive/MyDrive/modeleffnetb1newtourist.{epoch:02d}.h5'),
    tf.keras.callbacks.TensorBoard(log_dir='./drive/MyDrive/modeleffnetb1newtourist/logs'),
]

model2.fit_generator(train_batches2,steps_per_epoch = 163, epochs =3,callbacks=my_callbacks)

predictions = model2.predict(x=test_batches2,verbose=0)

cm2 = confusion_matrix(y_true=test_batches2.classes,y_pred=np.argmax(predictions,axis=-1))

cm_plot_labels = ['ain dxb','atlantis','burj al arab','burj_khalifa']
plot_confusion_matrix(cm=cm2,classes=cm_plot_labels,title='Confusion matrix')